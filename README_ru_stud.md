# ManosSeguras - Инструкция для студентов/разработчиков

---

## Предварительные требования

Перед началом работы установите следующее ПО:

### 1. Node.js (обязательно)

**Проверка наличия:**
```bash
node --version
```

Должна появиться версия типа `v18.x.x` или выше.

**Если Node.js не установлен:**

#### Вариант A: Через терминал (быстрее) ✅

**macOS (через Homebrew):**
```bash
# Установить Homebrew (если еще нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Node.js
brew install node

# Проверка
node --version
npm --version
```

**Windows (через Chocolatey):**
```bash
# Установить Chocolatey (если еще нет) - в PowerShell от администратора
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установить Node.js
choco install nodejs

# Проверка
node --version
npm --version
```

#### Вариант B: Скачать с сайта

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Установите (следуйте инструкциям установщика)
4. Перезапустите терминал
5. Проверьте установку: `node --version`

### 2. npm (устанавливается вместе с Node.js)

**Проверка:**
```bash
npm --version
```

Должна показать версию типа `9.x.x` или выше.

### 3. Git (опционально, для клонирования)

**Проверка:**
```bash
git --version
```

**Если Git не установлен:**
- macOS: `brew install git` или скачать с https://git-scm.com/
- Windows: https://git-scm.com/download/win
- Linux: `sudo apt-get install git`

---

## Скачивание проекта

### Клонирование через Git (рекомендуется)

```bash
# Клонируем репозиторий
git clone https://github.com/nvtoroy/acn3cv_PPPWEB_FINAL_ManosSeguras.git

# Переходим в папку проекта
cd acn3cv_PPPWEB_FINAL_ManosSeguras
```

---

## Установка и настройка

### Шаг 1: Установка зависимостей

Перейдите в папку `proyecto/`:

```bash
cd proyecto
```

Установите все необходимые библиотеки:

```bash
npm install
```

**Что установится:**
- express (веб-сервер)
- ejs (шаблонизатор)
- express-session (управление сессиями)
- bcrypt (шифрование паролей)
- sqlite3 (база данных)

Процесс займет 1-2 минуты. Появится папка `node_modules/`.

### Шаг 2: Инициализация базы данных

База данных создается автоматически при первом запуске, но можно инициализировать вручную:

```bash
node database/init.js
```

Это создаст файл `database/database.sqlite` и заполнит его тестовыми данными.

---

## Запуск проекта

### В папке `proyecto/` выполните:

```bash
node app.js
```

**Должно появиться:**
```
Сервер запущен на http://localhost:3000
База данных подключена
```

### Откройте браузер:

Перейдите по адресу: **http://localhost:3000**

---

## Тестовые пользователи

После инициализации БД доступны следующие пользователи (ВЫДУМКА! АКТУАЛИЗИРОВАТЬ!):

| Email                 | Пароль     | Роль                    |
|-----------------------|------------|-------------------------|
| admin@manoseguras.com | admin123   | Администратор           |
| juan@profesional.com  | prof123    | Профессионал (электрик) |
| maria@cliente.com     | cliente123 | Клиент                  |

**Используйте эти данные для входа на странице логина.**

---

## 📂 Структура проекта

```
proyecto/
├── app.js                    # Главный файл приложения (точка входа)
├── package.json              # Конфигурация и зависимости
│
├── config/                   # Конфигурации
│   ├── database.js           # Подключение к SQLite
│   └── session.js            # Настройки сессий
│
├── middleware/               # Промежуточные обработчики
│   ├── auth.js               # Проверка аутентификации
│   └── roles.js              # Проверка прав доступа
│
├── models/                   # Модели данных (работа с БД)
│   ├── User.js               # Пользователи
│   ├── Professional.js       # Профессионалы
│   ├── Solicitud.js          # Заявки на услуги
│   ├── Mensaje.js            # Сообщения в чате
│   └── Review.js             # Отзывы
│
├── controllers/              # Контроллеры (бизнес-логика)
│   ├── authController.js     # Логин/регистрация/выход
│   ├── homeController.js     # Главная страница
│   ├── professionalController.js  # Профессионалы
│   ├── profileController.js  # Личный кабинет
│   ├── solicitudController.js  # Заявки
│   └── adminController.js    # Админ-панель
│
├── routes/                   # Маршруты (URL → контроллер)
│   ├── index.js              # Главный роутер
│   ├── auth.js               # /login, /registro, /logout
│   ├── professional.js       # /profesionales, /profile/:id
│   ├── profile.js            # /profile (личный кабинет)
│   ├── solicitud.js          # /solicitar, /mis-solicitudes
│   └── admin.js              # /admin (админка)
│
├── views/                    # Шаблоны EJS (HTML)
│   ├── layouts/              # Общие шаблоны
│   ├── partials/             # Переиспользуемые части (header, footer)
│   ├── index.ejs             # Главная страница
│   ├── login.ejs             # Форма логина
│   ├── registro.ejs          # Форма регистрации
│   ├── profesionales.ejs     # Список профессионалов
│   ├── perfil-public.ejs     # Публичный профиль
│   ├── miperfil.ejs          # Личный кабинет
│   ├── solicitar.ejs         # Создание заявки
│   └── admin.ejs             # Админ-панель
│
├── public/                   # Статичные файлы
│   ├── css/                  # Стили
│   ├── js/                   # JavaScript фронтенда
│   └── img/                  # Изображения
│
├── database/
│   ├── schema.sql            # SQL схема таблиц
│   ├── seeds.sql             # Тестовые данные
│   ├── init.js               # Скрипт инициализации БД
│   └── database.sqlite       # Файл базы данных (генерируется)
│
└── utils/                    # Вспомогательные функции
    ├── validation.js         # Валидация данных
    └── helpers.js            # Общие хелперы
```

---

## Workflow разработки

### 1. Внесение изменений

Редактируйте файлы в любом редакторе (VS Code, Sublime, WebStorm и т.д.)

### 2. Перезапуск сервера

После изменений в коде:
1. Остановите сервер: `Ctrl + C`
2. Запустите снова: `node app.js`

**Совет:** Установите `nodemon` для автоматического перезапуска:
```bash
npm install -g nodemon
nodemon app.js
```

### 3. Просмотр изменений

Обновите страницу в браузере: `Ctrl + R` или `F5`

---

## Работа с базой данных

### Просмотр данных

**Через DB Browser for SQLite (GUI):**
1. Скачайте: https://sqlitebrowser.org/
2. Откройте файл `proyecto/database/database.sqlite`
3. Смотрите/редактируйте данные визуально

**Через командную строку:**
```bash
sqlite3 database/database.sqlite
.tables        # Показать все таблицы
SELECT * FROM users;    # Показать всех пользователей
.quit          # Выход
```

### Сброс базы данных

Если нужно начать с чистой БД:
```bash
# Удалить файл БД
rm database/database.sqlite

# Пересоздать с тестовыми данными
node database/init.js
```

---

## Решение проблем

### Проблема: `node: command not found`
**Решение:** Node.js не установлен. Установите с https://nodejs.org/

### Проблема: `npm install` не работает
**Решение:** 
1. Проверьте наличие файла `package.json` в текущей папке
2. Убедитесь что находитесь в папке `proyecto/`
3. Попробуйте: `npm cache clean --force` → `npm install`

### Проблема: `Error: listen EADDRINUSE :::3000`
**Решение:** Порт 3000 занят другим процессом.
1. Найдите процесс: `lsof -i :3000` (macOS/Linux)
2. Остановите: `kill -9 <PID>`
3. Или измените порт в `app.js`

### Проблема: База данных не создается
**Решение:**
1. Проверьте права на запись в папку `database/`
2. Запустите вручную: `node database/init.js`
3. Проверьте логи в консоли

### Проблема: Страницы не открываются (404)
**Решение:**
1. Проверьте что сервер запущен (`node app.js`)
2. Убедитесь что в консоли нет ошибок
3. Проверьте правильность URL

---

## Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск сервера
node app.js

# Запуск с автоперезагрузкой
nodemon app.js

# Инициализация БД
node database/init.js

# Проверка версий
node --version
npm --version

# Просмотр установленных пакетов
npm list --depth=0

# Обновление зависимостей
npm update
```

---

## Безопасность

**Важно для продакшена (если будете деплоить):**

1. **Измените секрет сессии** в `config/session.js`

---

## Деплой (опционально)

Если хотите разместить проект онлайн:

### Heroku (бесплатно)
```bash
# Установить Heroku CLI
npm install -g heroku

# Логин
heroku login

# Создать приложение
heroku create manoseguras-app

# Деплой
git push heroku main
```

### Railway (бесплатно)
1. Зарегистрируйтесь на https://railway.app/
2. Подключите GitHub репозиторий
3. Railway автоматически деплоит проект

---
